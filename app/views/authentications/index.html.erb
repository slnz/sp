<div class="row">
  <div class="col-md-4">
      <% if @authentications || logged_in? %>
      <% unless @authentications.empty? && current_user.globallyUniqueID.blank?  %>
        <div id="currentservices">
          <h3>You are setup for access with these services:</h3>
          <div class="authentications">
            <% if current_user.globallyUniqueID.present? %>
              <div class="authentication">
                <%= image_tag "relay.png", :size => "32x32" %>
                <div class="provider">Relay</div>
              </div>
            <% end %>
            <% for authentication in @authentications %>
              <div class="authentication">
                <%= image_tag "#{authentication.provider}_32.png", :size => "32x32" %>
                <div class="provider"><%= authentication.provider_name %></div>
                <%= link_to "X", authentication, :confirm => 'Are you sure you want to remove this authentication option?', :method => :delete, :class => "remove" %>
              </div>
            <% end %>
            <div class="clear"></div>
          </div>
        </div>
      <% end %>
      <div class="text-center">
        <h5>Add another service to sign in with</h5>
    <% else %>
      <div class="text-center">
        <h5>Sign in through these services</h5>
    <% end %>
      <div class="row">
        <div class="col-xs-offset-1 col-xs-10">
        <!-- <a href="/auth/twitter" class="auth_provider">
          <%= image_tag "twitter_64.png", :size => "64x64", :alt => "Twitter" %>
          Twitter
        </a> -->
        <a href="<%= CASClient::Frameworks::Rails::Filter.login_url(controller) %>" class="btn btn-lg btn-social btn-github btn-block">
        <%= fa_icon 'key' %>
          Sign in with The Key
          <!--%= image_tag "relay-sign-in.png", :alt => "Relay" %-->
        </a>
        <a href="/auth/facebook" class="btn btn-lg btn-social btn-facebook btn-block">
        <%= fa_icon 'facebook' %>
          Sign in with Facebook
          <!--%= image_tag "facebook-sign-in.png", :alt => "Facebook" %-->
        </a>
        <!-- ><a href="/auth/google_apps" class="auth_provider">
          <%= image_tag "google_apps_64.png", :size => "64x64", :alt => "Google" %>
          Google
        </a> -->
        </div>
      </div>
    </div>
  </div>
  <div class="col-md-8">
    <% unless logged_in? %>
      <strong>Have you applied for a Summer Project or STINT or signed up for a Cru Conference before?</strong>
      <p>That username and login has been moved to our Relay Sign-In System. The Relay button will take you to it's Sign-In screen.</p>
      <strong>Haven't registered yet? Pick a button.</strong>
      <p>Create a new Relay account or sign in with Facebook.</p>
      <p>If you know you logged in before but don't remember your password, the 'forgot password' link has been moved to the Relay login system as well.
      Please recover your password instead of creating a new account.</p>
    <% end %>
  </div>
</div>
